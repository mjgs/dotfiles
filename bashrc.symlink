# exports
[ -n "$DEBUG" ] && echo "sourcing $HOME/.exports"
source $HOME/.exports

# functions
[ -n "$DEBUG" ] && echo "sourcing $HOME/.functions"
source $HOME/.functions

# aliases
[ -n "$DEBUG" ] && echo "sourcing $HOME/.aliases"
source $HOME/.aliases

