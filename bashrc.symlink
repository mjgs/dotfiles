# exports
[ -n "$VERBOSE" ] && echo "sourcing $HOME/.exports/exports"
source $HOME/.exports/exports

# functions
[ -n "$VERBOSE" ] && echo "sourcing $HOME/.functions/functions"
source $HOME/.functions/functions

# aliases
[ -n "$VERBOSE" ] && echo "sourcing $HOME/.aliases/aliases"
source $HOME/.aliases/aliases

