# exports
[ -n "$VERBOSE" ] && echo "sourcing $HOME/.exports"
source $HOME/.exports

# functions
[ -n "$VERBOSE" ] && echo "sourcing $HOME/.functions"
source $HOME/.functions

# aliases
[ -n "$VERBOSE" ] && echo "sourcing $HOME/.aliases"
source $HOME/.aliases

